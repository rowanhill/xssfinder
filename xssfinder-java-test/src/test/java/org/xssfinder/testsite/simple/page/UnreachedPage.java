package org.xssfinder.testsite.simple.page;

import org.xssfinder.Page;

/**
 * This page is never reached, because the method that traverses to it throws an exception. The page doesn't actually
 * exist in the test web app.
 */
@Page
public class UnreachedPage {
}
